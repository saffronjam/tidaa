#include <stdio.h>
#include "interaction.h"


void Get_player_tile(int *maxValue, int testmax){
    for(int i=0; i<*maxValue;i++){
        
    }
    printf("what is the value of max? it's %d ofcorse\n",*maxValue);
    printf("what is the value of testmax= %d\n",testmax);
}